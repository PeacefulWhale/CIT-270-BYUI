apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: default
spec:
  containers:
  - image: busybox
    command:
      - sleep
      - "-m"
      - "5"
    imagePullPolicy: IfNotPresent
    name: busybox
  restartPolicy: Always